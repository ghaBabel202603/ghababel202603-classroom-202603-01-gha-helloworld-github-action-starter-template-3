name: HELLO WORLD
on:
  push:
permissions:  
  checks: write
  actions: read
  contents: read
jobs:
  Saludo:
    runs-on: ubuntu-latest
    steps:
    - name: echo-hello-world
      run: echo '¡Hola, mundo!'
